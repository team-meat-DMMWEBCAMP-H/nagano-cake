<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <p>ジャンル検索欄</p>
      <!--%= render ジャンル検索 %-->
    </div>
    <div class="col-md-8 offset-md-1">
      <h1>商品一覧</h1>
        <% @items.each do |item| %>
      <div class="cok-3 mt-3">
        <!--%=link_to item_path(item) do %>
          <!--%= image_tag item.get_profile_image(100,100),fallback: "no_image.jpg" %-->
        <!--% end %><br-->
        <strong class><%= item.name %></strong><br>
        <span class=><%= item.price %>円</span><br>
        <% end %>
      </div>
    </div>
  </div>
</div>
